version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=WarehouseManagement_Identity
      - WarehouseManagement_DB=WarehouseManagement_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
